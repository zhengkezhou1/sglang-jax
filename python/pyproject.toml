[build-system]
requires = ["setuptools>=64.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sglang-jax"
version = "0.0.3"
description = "JAX backend for SGL"
readme = "README.md"
requires-python = ">=3.12,<3.14"
license = { file = "LICENSE" }

dependencies = [
    "fastapi~=0.116.1",
    "flax==0.12.4",
    "huggingface-hub~=0.34.3",
    "jinja2~=3.1.6",
    "llguidance~=1.3.0",
    "modelscope~=1.28.2",
    "msgpack-python~=0.5.6",
    "numpy~=2.2.6",
    "orjson~=3.11.1",
    "pillow~=11.3.0",
    "psutil~=7.0.0",
    "pydantic~=2.11.7",
    "python-multipart>=0.0.20",
    "pyzmq~=27.0.1",
    "requests~=2.32.4",
    "safetensors~=0.6.1",
    "setproctitle~=1.3.6",
    "tiktoken>=0.10.0",
    "transformers~=4.57.1",
    "typing-extensions~=4.14.1",
    "uvicorn~=0.35.0",
    "uvloop~=0.21.0",
    "psutil",
    "httpx",
    "openai",
    "pandas",
    "aiohttp",
    "pybase64",
    "partial_json_parser",
    "pathwaysutils",
    "omegaconf",
    "imageio[ffmpeg]",
    "librosa",
    "datasets",
]

[project.optional-dependencies]
dev = [
]
tpu = [
    "jax[tpu]==0.8.1",
]
gpu = [
    "jax[cuda12]==0.8.1",
]
cpu = [
    "jax[cpu]==0.8.1",
]
all = [
    "jax[tpu]==0.8.1",
]
multimodal = [
    "torch",
    "torchvision",
    "torchaudio",
    "decord",
    "msgpack==1.0.0",
    "qwix",
    "soundfile",
]

[tool.setuptools.packages.find]
exclude = [
    "assets*",
    "benchmark*",
    "docs*",
    "dist*",
    "playground*",
    "scripts*",
    "tests*",
]

[tool.wheel]
exclude = [
    "assets*",
    "benchmark*",
    "docs*",
    "dist*",
    "playground*",
    "scripts*",
    "tests*",
]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort (handled by pre-commit isort hook)
    # "I",
    # flake8-logging-format
    "G",
]
ignore = [
    # line too long handled by Black
    "E501",
    # star imports
    "F405", "F403",
    # lambda expression assignment
    "E731",
    # zip without `strict=`
    "B905",
    # Loop control variable not used within loop body
    "B007",
    # Function call in argument defaults (FastAPI uses this pattern)
    "B008",
]

[tool.ruff.lint.per-file-ignores]
# Pallas kernels: Python for-loops are statically unrolled at trace time,
# so closures capturing loop variables is intentional and correct.
"sgl_jax/srt/kernels/fused_moe/v1/kernel.py" = ["B023"]

[tool.ruff.format]
docstring-code-format = true

[tool.mypy]
files = [
    "python/sgl_jax/srt/models/grok.py"
]

python_version = "3.12"

show_error_codes = true

warn_unused_configs = true
warn_redundant_casts = true

allow_redefinition = true

# Gradual rollout settings
check_untyped_defs = true
no_implicit_optional = true

# Don't be too strict with third-party dependencies
# (JAX / transformers / tiktoken / numpy, etc.)
ignore_missing_imports = true
follow_imports = "silent"

# Disable some noisy error codes for now
# (we can enable them later as the codebase gets cleaner)
disable_error_code = [
    "attr-defined",
    "name-defined",
    "annotation-unchecked"
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cpu" }
torchvision = { index = "pytorch-cpu" }
torchaudio = { index = "pytorch-cpu" }
